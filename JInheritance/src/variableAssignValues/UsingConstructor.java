package variableAssignValues;

public class UsingConstructor {
    //int sid;String name; Character grade;
   // UsingConstructor(int id, String name; char g)
   // {sid = id; sname = name;grade=g;}
    {
    //    System.out.println(sid+""+sname+""+grade);
    }
}
